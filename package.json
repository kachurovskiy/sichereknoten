{
  "name": "sichere-knoten",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve-docs.mjs",
    "generate:municipalities": "node scripts/generate-municipalities.mjs",
    "build": "tsc --noEmit && node scripts/build-docs.mjs",
    "serve:docs": "node scripts/serve-docs.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2"
  }
}
