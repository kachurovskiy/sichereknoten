{
  "name": "sichere-knoten",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && node scripts/build-docs.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "serve:docs": "vite docs --host 127.0.0.1"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2",
    "vite": "^7.0.0"
  }
}
